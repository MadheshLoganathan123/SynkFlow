name: New Pipeline (Manual Trigger)

on:
  repository_dispatch:
    types: [new_pipeline_manual_trigger]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run Pipeline
        run: |
          echo "🚀 Pipeline triggered manually via SynkFlow dashboard!"
          echo "Event type: ${{ github.event.action }}"
          # Add your actual build/test/deploy steps here
